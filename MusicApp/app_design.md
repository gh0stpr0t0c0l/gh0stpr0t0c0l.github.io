Ok so the app should have these basic features:
 - store youtube links to all of my songs plus display album art, titles, artists, etc (preferrably not storing the images locally).
 - access youtube content (via brave maybe?) so contributors are compensated but I don't get ads. This needs to be legal.
 - be hostable as a github website so I can access via my phone
 - have a phone and laptop web interface
 - have a shuffle feature within playlists
 - have a play/pause feature that quickly fades the music in/out.
 - have a playlist feature that allows playlist creation, deletion, viewing
 - allow for adding the same song to multiple playlists (this is a feature that Brave doesn't have)
 - have a helper app that can one-time transfer all of my playlists from the spotify API to the new web app
 - I'll come up with a name and branding later
 - skip song, prev song (that when first pressed restarts the current song)
 - if possible, some sort of search feature that searches youtube API for songs 
 - no videos in app
 - make sure the next song plays automatically after the first...don't worry about blocking ads...I think Brave will take care of that.